title: "MikeGuides Blog"
description: "Practical posts on pricing, profit, and AI systems for small businesses and solo founders."
url: "https://blog.pricingforprofit.co"

# Optional but recommended: default social image for posts without featured_image
og_image: "/assets/images/cover/welcome.1280x720.jpg"

markdown: kramdown

# You have custom layouts, so 'theme' is optional. You can keep it or remove it.
# theme: minima

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

collections:
  slugs:
    output: true
    permalink: /slugs/:name/
